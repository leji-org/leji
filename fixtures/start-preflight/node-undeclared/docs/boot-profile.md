# Boot Profile

## Identity

An adopted layer a joiner has just cloned.

## Setup

For a person preparing a fresh clone before opening an agent: install the repository's dependencies with its package manager, then run `leji start`.

## Loading

Read `docs/domain/` before any task.

## Posture

- Stop and ask before destructive changes.

## Maintenance

Decisions are recorded in `docs/decisions/`.
