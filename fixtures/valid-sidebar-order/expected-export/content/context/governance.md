# Shared context

The later category under the shared label: its one entry sits at position 0 and still
renders after every entry the system index declared.

```leji-index
- path: docs/shared/approvals.md
```
