# Overview

A directory inside the layer whose `README.md` is a symlink to a file outside it:
[the section](section/).

The directory is contained, and the README exists, so only a containment check on
the README itself refuses this target.
