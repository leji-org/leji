# Overview

A root-absolute target that rootPath does not carry: [gone](/domain/missing.md).

The repository-root spelling of this document's own path is the same mistake, so
it is not repeated here: one failure mode, one fixture.
