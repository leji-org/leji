<!-- Stale onboarding artifact, planted by this fixture at the pre-1.4 location
     (<rootPath>/.leji/onboarding-brief.md). Placeholder bytes standing for
     whatever a pre-1.4 release left in that tree: inert data this version
     neither reads nor writes, present only so the contract can pin that it
     stays exactly as it is.

     LEJI-TRUST-CANARY — these bytes are inert: no run may serve or export them. -->

# Onboarding brief for the agent

**Working mode:** solo

Placeholder body at the old location. Nothing consumes this file.
