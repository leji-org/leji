# generated by leji ci (managed) v2
version: 2.1
jobs:
  leji-validate:
    docker:
      - image: python:3.12
    steps:
      - checkout
      - run: pip install 'leji>=1,<2'
      - run: leji validate
      - run: leji index --check
workflows:
  leji:
    jobs:
      - leji-validate
