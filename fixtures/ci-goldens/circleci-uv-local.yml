# generated by leji ci (managed) v2
version: 2.1
jobs:
  leji-validate:
    docker:
      - image: python:3.12
    steps:
      - checkout
      # uv is installed unpinned here; pin it if your project pins it.
      - run: pip install uv && uv sync --locked
      - run: uv run leji validate
      - run: uv run leji index --check
workflows:
  leji:
    jobs:
      - leji-validate
