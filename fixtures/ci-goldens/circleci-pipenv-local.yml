# generated by leji ci (managed) v2
version: 2.1
jobs:
  leji-validate:
    docker:
      - image: python:3.12
    steps:
      - checkout
      # pipenv is installed unpinned here; pin it if your project pins it.
      - run: pip install pipenv && pipenv install --dev
      - run: pipenv run leji validate
      - run: pipenv run leji index --check
workflows:
  leji:
    jobs:
      - leji-validate
