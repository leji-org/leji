# generated by leji ci (managed) v2
version: 2.1
jobs:
  leji-validate:
    docker:
      - image: python:3.12
    steps:
      - checkout
      # poetry is installed unpinned here; pin it if your project pins it.
      - run: pip install poetry && poetry install
      - run: poetry run leji validate
      - run: poetry run leji index --check
workflows:
  leji:
    jobs:
      - leji-validate
