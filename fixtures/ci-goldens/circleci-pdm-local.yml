# generated by leji ci (managed) v2
version: 2.1
jobs:
  leji-validate:
    docker:
      - image: python:3.12
    steps:
      - checkout
      # pdm is installed unpinned here; pin it if your project pins it.
      - run: pip install pdm && pdm install
      - run: pdm run leji validate
      - run: pdm run leji index --check
workflows:
  leji:
    jobs:
      - leji-validate
