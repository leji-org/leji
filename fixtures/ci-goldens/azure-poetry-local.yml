# generated by leji ci (managed) v2
trigger:
  - main
pool:
  vmImage: ubuntu-latest
steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.12'
  # poetry is installed unpinned here; pin it if your project pins it.
  - script: pip install poetry && poetry install
    displayName: install
  - script: poetry run leji validate
    displayName: leji validate
  - script: poetry run leji index --check
    displayName: leji index --check
