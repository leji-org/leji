# >>> leji ci (managed) >>>
leji-validate:
  stage: .pre
  image: node:22
  script:
    - corepack enable && pnpm install --frozen-lockfile
    - pnpm exec leji validate
    - pnpm exec leji index --check
# <<< leji ci (managed) <<<
