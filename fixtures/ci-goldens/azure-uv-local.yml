# generated by leji ci (managed) v2
trigger:
  - main
pool:
  vmImage: ubuntu-latest
steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.12'
  # uv is installed unpinned here; pin it if your project pins it.
  - script: pip install uv && uv sync --locked
    displayName: install
  - script: uv run leji validate
    displayName: leji validate
  - script: uv run leji index --check
    displayName: leji index --check
