# generated by leji ci (managed) v2
trigger:
  - main
pool:
  vmImage: ubuntu-latest
steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.12'
  # pdm is installed unpinned here; pin it if your project pins it.
  - script: pip install pdm && pdm install
    displayName: install
  - script: pdm run leji validate
    displayName: leji validate
  - script: pdm run leji index --check
    displayName: leji index --check
