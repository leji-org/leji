# generated by leji ci (managed) v2
version: 2.1
jobs:
  leji-validate:
    docker:
      - image: node:22
    steps:
      - checkout
      - run: corepack enable && pnpm install --frozen-lockfile
      - run: pnpm exec leji validate
      - run: pnpm exec leji index --check
workflows:
  leji:
    jobs:
      - leji-validate
