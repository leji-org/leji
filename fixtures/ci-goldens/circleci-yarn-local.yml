# generated by leji ci (managed) v2
version: 2.1
jobs:
  leji-validate:
    docker:
      - image: node:22
    steps:
      - checkout
      - run: corepack enable && yarn install --frozen-lockfile
      - run: yarn leji validate
      - run: yarn leji index --check
workflows:
  leji:
    jobs:
      - leji-validate
