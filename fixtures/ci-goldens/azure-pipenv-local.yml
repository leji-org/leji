# generated by leji ci (managed) v2
trigger:
  - main
pool:
  vmImage: ubuntu-latest
steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.12'
  # pipenv is installed unpinned here; pin it if your project pins it.
  - script: pip install pipenv && pipenv install --dev
    displayName: install
  - script: pipenv run leji validate
    displayName: leji validate
  - script: pipenv run leji index --check
    displayName: leji index --check
