# generated by leji ci (managed) v2
version: 2.1
jobs:
  leji-validate:
    docker:
      - image: oven/bun:1
    steps:
      - checkout
      - run: bun install --frozen-lockfile
      - run: bun run leji validate
      - run: bun run leji index --check
workflows:
  leji:
    jobs:
      - leji-validate
