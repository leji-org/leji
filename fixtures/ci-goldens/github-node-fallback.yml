# generated by leji ci (managed) v2
name: leji
on: [push, pull_request]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npx -y @leji-org/leji@1 validate
      - run: npx -y @leji-org/leji@1 index --check
