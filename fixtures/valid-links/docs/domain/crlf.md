# CRLF

This document is stored with CRLF line endings, so a scanner that measures a
fence or a code span against the line terminator sees them here.

A link back: [the overview](overview.md).

```text
[gone](nowhere.md)
```

An unbroken code span: `[gone](nowhere.md)`.
