[project]
name = "demo"
version = "0.1.0"
dependencies = [
    # leji
    "requests",
]

[project.optional-dependencies]
# leji
test = ["pytest"]  # leji

[dependency-groups]
dev = [
    # leji>=1
]

[tool.uv]
# leji = "1"
dev-dependencies = []
