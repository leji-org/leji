---
import { getCollection, render } from 'astro:content';
import Doc from '../layouts/Doc.astro';

const [doc] = await getCollection('adoption');
const { Content } = await render(doc);
---

<Doc
   kind="guide"
   title="Adopting Leji · Leji"
   description="How to adopt Leji in a monorepo, a multi-repo organization, or alongside an existing context repo, without moving the documents you already have."
>
   <Content />
</Doc>
