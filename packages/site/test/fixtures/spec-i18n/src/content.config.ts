// The site's own collection definition over this fixture's content: the loader's base
// is resolved against the project root, so the same declaration reads whichever tree
// it is built in.
import { specI18n } from '@site/content.config';

export const collections = { specI18n };
