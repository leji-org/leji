# Examples

Two deliberately boring reference context layers: monorepo/ (single repo at `indexed` conformance, fully materialized and schema-valid) and multi-repo/ (federated multi-repo setup: dedicated context repo, docs-only submodule consumers, a sibling mount).
